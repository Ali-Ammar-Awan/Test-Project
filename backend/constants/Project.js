const Project = Object.freeze({

  MESSAGES: {
    CREATING_PROJECT_FAILED: 'Somethhing went wrong while creating the project',
    DO_NOT_HAVE_ACCESS:'Does not have access to create project',
    FAILED_FETCH:'Failed to fetch projects'
  }

});

module.exports = Project;
